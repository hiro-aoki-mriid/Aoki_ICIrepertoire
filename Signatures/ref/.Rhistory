#Detect signature genes from BD target panel
library(dplyr)
##BD target panel
BD_genes <- read.table("BD_genes_plus10.txt", header = TRUE)
BD_genes <- as.vector(BD_genes$Genesymbol)
##signatures
BD_genes <- read.table("Daniel_TexKLR.txt", header = TRUE)
##BD target panel
BD_genes <- read.table("BD_genes_plus10.txt", header = TRUE)
BD_genes <- as.vector(BD_genes$Genesymbol)
##signatures
table.genes <- read.table("Daniel_TexKLR.txt", header = TRUE)
View(table.genes)
##signatures
table.genes <- read.table("Daniel_TexKLR.txt", header = FALSE)
View(table.genes)
table.genes <- as.vector(table.genes$V1)
##Extract
sig.genes <- as.data.frame(intersect(BD_genes, as.vector(table.genes$Gene)))
##Extract
sig.genes <- as.data.frame(intersect(BD_genes, table.genes))
sig.genes
names(sig.genes) <- "Genes"
name.output <- "Sig.TexKLR.txt"
write.table(sig.genes, name.output, row.names = FALSE)
